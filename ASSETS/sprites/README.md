# Sprite Assets

Place exported sprite JSON files from the Sprite Workshop here.

Recommended workflow:

1. Open `TOOLS/sprite_workshop.html` in a browser
2. Draw or import a sprite
3. Use `Download JSON`
4. Move the exported file into this directory

The current export format includes:

- sprite name
- width and height
- fixed palette definition
- flat pixel array using palette indexes
